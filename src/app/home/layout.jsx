import React from 'react'

export const metadata = {
    title: "Home | Feastify",
};

const layout = ({ children }) => {
    return (
        <div>
            {children}
        </div>
    )
}

export default layout
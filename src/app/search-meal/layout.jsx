import React from 'react'

export const metadata = {
    title: "Search-Meal | Feastify",
}; 

export default function layout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}

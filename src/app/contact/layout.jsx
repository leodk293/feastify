import React from 'react'

export const metadata = {
    title: "Contact | Feastify",
};

export default function layout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}
